export interface City {
  id: string;
  name: string;
  countryId: string;
}

export interface CreateCityDTO {
  id: string;
  name: string;
  countryId: string;
}

export interface UpdateCityDTO {
  id: string;
  name: string;
  countryId: string;
}
